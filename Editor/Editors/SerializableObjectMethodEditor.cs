using Assets.Scripts.Core;

namespace Assets.Scripts.Editor.Editors {

    /*public class SerializableObjectMethodEditor : BaseCustomEditor<SerializableObjectMethod> {

        public override void Draw(SerializableObjectMethod model) {
            Label();

            using (Indent()) {
                DrawProperty(property => property.Behaviour);

                if (model.Behaviour != null) {
                    MethodDropdown(model.Behaviour, target => target.Method);
                }
            }
        }
    }*/
}